# Source pinning

The shadow Compose file in this repository builds the checked-out source tree. External deployment repositories must pin their remote build context to a reviewed immutable Caddy UI commit. Do not track `main` automatically during the cutover observation period.
