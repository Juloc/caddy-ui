name: Archived .NET shadow beta publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The isolated beta channel is archived."
          echo "Use the stable release workflow and deploy/docker-compose.yml."
